#include <bits/stdc++.h>

using namespace std;

int const N = 1e6 + 5;

int t;
int n, q;
int a[N];


int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0); cout.tie(0);

    cin >> t;
    
    cin >> n;
    for (int i = 1; i <= n; i++) cin >> a[i];
    cin >> q;

    while (q--)
    {
        int l, r;
    }

    return 0;
}