#include <bits/stdc++.h>

using namespace std;

int const N = 109;

struct Edge
{
    int v, w;
    int j;
};

int n;
int k;
int a;
int b;

vector<Edge> adj[N];

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0); cout.tie(0);

    cin >> a >> b;
    cin >> n >> k;

    while (k--)
    {
        int u, v, w;
        cin >> u >> v >> w;

        
    }

    return 0;
}