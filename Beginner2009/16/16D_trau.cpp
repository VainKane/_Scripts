#include <bits/stdc++.h>

using namespace std;

int const N = 1e5 + 10;

int n;
int m;
int a[N];

int v;
int k;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0); cout.tie(0);

    cin >> n >> m;
    for (int i = 1; i <= n - 1; i++) cin >> a[i];

    while (m--)
    {
        cin >> v >> k;

        int res = 0;
    }

    return 0;
}