#include <bits/stdc++.h>

using namespace std;

int const N = 2e5 + 10;

int n;
int m;

int a[N];

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0); cout.tie(0);

    cin >> n >> m;
    for (int i = 1; i <= n; i++) cin >> a[i];

    while (m--)
    {
        long long t;
        int l, r;

        cin >> t >> l >> r;

        
    }

    return 0;
}