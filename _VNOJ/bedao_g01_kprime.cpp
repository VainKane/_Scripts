#include <bits/stdc++.h>

using namespace std;

#define FOR(i, a, b) for (int i = (a), _b = (b); i <= _b; i++)

int const N = 1e7 + 5;

bool prime[N];

void Sieve(int l, int r)
{
    memset(prime, true, sizeof prime);

    FOR(i, 2, sqrt(r)) if (prime[i])
    {

    }
}

int main()
[
    ios_base::sync_with_stdio(false);
    cin.tie(0); cout.tie(0);

    return 0;
]