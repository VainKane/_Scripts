#include <bits/stdc++.h>

using namespace std;

int const N = 1e5 + 5;

#define FOR(i, a, b) for (int i = (a), _b = (b); i <= _b; i++)

long long n;
int k;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0); cout.tie(0);

    cin >> n >> k;
    

    return 0;
}