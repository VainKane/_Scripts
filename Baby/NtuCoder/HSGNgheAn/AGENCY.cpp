#include <bits/stdc++.h>

using namespace std;

int n; // so dai ly
int m; // so xe

int pos[409]; // toa do cac dai ly
int s[409]; // diem bat dau
int f[409]; // diem ket thuc
int c[409]; // xang dung / doan duong
int r[409]; // lan do xang

int main()
{
    cin >> n >> m;

    for (int i = 0; i < n; i++)
    {
        cin >> pos[i];
    }

    for (int i = 0; i < m; i++)
    {
        cin >> s[i] >> f[i] >> c[i] >> r[i];
    }

    
}