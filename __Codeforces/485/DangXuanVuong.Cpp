#include <bits/stdc++.h>

using namespace std;

const int N = (int)2e5 + 1;
int n, a[N];

void read()
{
    cin >> n;
    for(int i = 1; i <= n; ++i)
        cin >> a[i];
}

void solve()
{
    sort()
}

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0); cout.tie(0);
    read(); solve();
    return 0;
}