#include <bits/stdc++.h>

using namespace std;

int m;
int n;
int k;

int s;

int a[(int)1e5 + 10];
int b[(int)1e5 + 10];

int main()
{
    cin >> m >> n >> k;

    for (int i = 0; i < m; i++)
    {
        cin >> a[i];
    }

    for (int i = 0; i < n; i++)
    {
        cin >> b[i];
    }

    while (k--)
    {
        cin >> s;

        for (int i = 0; i < n; i++)
        {
            if (a[i])
        }
    }

    return 0;
}
