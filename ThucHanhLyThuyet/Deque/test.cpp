#include <bits/stdc++.h>

using namespace std;

deque<int> q;
int n;
int k;
int a[(int)1e5 + 10];

int main()
{
    cin >> n >> k;

    for (int i = 0; i < n; i++)
    {
        cin >> a[i];
    }

    q.push_back(0);

    for (int i = 1; i < n; i++)
    {
        for (int j = 1; j < k; j++)
        {
            
        }
    }

    return 0;
}