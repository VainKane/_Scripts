for (int i = 0; i < s.length() && k > 0; i++)
    // {
    //     int ind = min_element(s.begin() + i, s.begin() + k + 1) - s.begin();
    //     s.erase(i + 1, ind);
    //     k -= ind - i;
    // }

    // cout << s << '\n';