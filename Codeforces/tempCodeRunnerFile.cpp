if (cows[j].cards[i] < cows[j].cards[i - 1] || cows[j].cards[i] < pre);
                // {
                //     j = n + 1;
                //     i = m + 1;

                //     cout << -1 << '\n';
                // }