#include <bits/stdc++.h>

using namespace std;

int t;

int m;
int n;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0); cout.tie(0);

    cin >> t;

    while (t--)
    {
        cin >> m >> n;
        
        for (   )
    }

    return 0;
}